/*
 * rij vooruit zolang de touchsensor niet word ingedrukt
 */

task main()
{
 while(true)
 {
  SetSensorTouch(S1);
  OnFwd(OUT_AC, 50);
  while(SENSOR_1 != 0) // != betekend is niet
  {
   OnRev(OUT_AC, 50);
  }
 }
}
